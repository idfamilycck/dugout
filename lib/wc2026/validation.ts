// 엔진 검증 수치(표시용 단일 소스).
//
// 이 앱의 카운터팩추얼("당신이 지휘했다면 실제 패배가 승리가 됐을 것이다")은 엔진이
// 현실을 얼마나 재현하느냐에 신뢰가 걸려 있다. 그래서 엔진을 실제 2026 월드컵 103경기에
// "무개입"으로 돌려(실제 선발 라인업, 0~90분) 실제 결과를 얼마나 재현하는지 측정했다.
//
// 아래 값은 lib/engine/validation.test.ts가 매 실행마다 재계산해 검증한다(시드 고정이라
// 결정론적). 엔진을 바꿔 수치가 움직이면 그 테스트가 실패하므로, 화면·기획서에 쓰는
// 이 숫자가 실측과 어긋날 수 없다.

export const ENGINE_VALIDATION = {
  /** 검증에 쓴 실제 경기 수. */
  matches: 103,
  /** 경기당 시뮬레이션 시드 수(결과 최빈값으로 예측). */
  seedsPerMatch: 15,
  /** 승/무/패 전체 재현율(%). 무작위 기준선 33% 대비. */
  outcomeRatePct: 51.5,
  /** 승부가 갈린 경기(무승부 제외)에서 이긴 팀 재현율(%). 대표 지표. */
  decisiveWinRatePct: 70.7,
  /** 팀당 평균 득점 오차(골). */
  goalMae: 0.97,
} as const;
